import axios from'axios';import{useEffect,useState,type FormEvent}from'react';import{adminContentApi}from'./adminContentApi';import type{Announcement,Faq}from'./publicContentApi';
export function AdminContentPage(){const[announcements,setAnnouncements]=useState<Announcement[]>([]);const[faqs,setFaqs]=useState<Faq[]>([]);const[editingAnnouncement,setEditingAnnouncement]=useState<Announcement|null>(null);const[editingFaq,setEditingFaq]=useState<Faq|null>(null);const[error,setError]=useState('');async function load(){const[a,f]=await Promise.all([adminContentApi.announcements(),adminContentApi.faqs()]);setAnnouncements(a);setFaqs(f)}useEffect(()=>{let active=true;Promise.all([adminContentApi.announcements(),adminContentApi.faqs()]).then(([a,f])=>{if(active){setAnnouncements(a);setFaqs(f)}}).catch(e=>{if(active)setError(message(e))});return()=>{active=false}},[]);async function saveAnnouncement(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,d=new FormData(form),body={title:d.get('title'),slug:d.get('slug'),summary:d.get('summary'),content:d.get('content')};try{if(editingAnnouncement)await adminContentApi.updateAnnouncement(editingAnnouncement,body);else await adminContentApi.createAnnouncement(body);setEditingAnnouncement(null);form.reset();await load()}catch(x){setError(message(x))}}async function saveFaq(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,d=new FormData(form),body={question:d.get('question'),answer:d.get('answer'),displayOrder:Number(d.get('displayOrder'))};try{if(editingFaq)await adminContentApi.updateFaq(editingFaq,body);else await adminContentApi.createFaq(body);setEditingFaq(null);form.reset();await load()}catch(x){setError(message(x))}}async function announcementAction(v:Announcement,kind:'publish'|'archiveAnnouncement'){try{await adminContentApi[kind](v);await load()}catch(e){setError(message(e))}}async function faqArchive(v:Faq){try{await adminContentApi.archiveFaq(v);await load()}catch(e){setError(message(e))}}return <section className="admin-workspace content-admin"><header><p className="eyebrow">İçerik</p><h1>Public içerik yönetimi</h1><p>Duyuru ve sıkça sorulan soruları yönetin.</p></header>{error&&<p role="alert" className="status status--error">{error}</p>}<div className="content-grid"><form className="management-card" key={editingAnnouncement?.id??'new-announcement'} onSubmit={saveAnnouncement}><h2>{editingAnnouncement?'Duyuruyu düzenle':'Yeni duyuru'}</h2><label>Başlık<input name="title" defaultValue={editingAnnouncement?.title??''} required/></label><label>URL adı<input name="slug" pattern="[a-z0-9]+(-[a-z0-9]+)*" defaultValue={editingAnnouncement?.slug??''} required/></label><label>Özet<textarea name="summary" defaultValue={editingAnnouncement?.summary??''} required/></label><label>İçerik<textarea name="content" rows={8} defaultValue={editingAnnouncement?.content??''} required/></label><button>{editingAnnouncement?'Güncelle':'Taslak oluştur'}</button></form><section className="management-card"><h2>Duyurular</h2>{announcements.map(v=><article className="content-row" key={v.id}><div><strong>{v.title}</strong><span>{v.status}</span></div><div>{v.status==='DRAFT'&&<><button className="secondary" onClick={()=>setEditingAnnouncement(v)}>Düzenle</button><button onClick={()=>void announcementAction(v,'publish')}>Yayınla</button></>}{v.status!=='ARCHIVED'&&<button className="danger" onClick={()=>void announcementAction(v,'archiveAnnouncement')}>Arşivle</button>}</div></article>)}</section><form className="management-card" key={editingFaq?.id??'new-faq'} onSubmit={saveFaq}><h2>{editingFaq?'SSS düzenle':'Yeni SSS'}</h2><label>Soru<input name="question" defaultValue={editingFaq?.question??''} required/></label><label>Yanıt<textarea name="answer" rows={5} defaultValue={editingFaq?.answer??''} required/></label><label>Sıra<input name="displayOrder" type="number" min="0" defaultValue={editingFaq?.displayOrder??0} required/></label><button>{editingFaq?'Güncelle':'SSS oluştur'}</button></form><section className="management-card"><h2>SSS kayıtları</h2>{faqs.map(v=><article className="content-row" key={v.id}><div><strong>{v.question}</strong><span>Sıra {v.displayOrder} · {v.active?'Aktif':'Arşiv'}</span></div>{v.active&&<div><button className="secondary" onClick={()=>setEditingFaq(v)}>Düzenle</button><button className="danger" onClick={()=>void faqArchive(v)}>Arşivle</button></div>}</article>)}</section></div></section>}
function message(e:unknown){return axios.isAxiosError(e)?e.response?.data?.message??'İşlem tamamlanamadı.':'İşlem tamamlanamadı.'}
